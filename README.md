# Tweet Collector
This program collects tweets from Twitter to perform stock price prediction.

## Dependencies
All dependencies are in the `requirements.txt` file. To install them, type this command:

`> pip install -r requirements.txt`

## Author
- Dimitri K. Sifoua